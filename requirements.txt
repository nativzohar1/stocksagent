# Decoupling Hunter scanner — pinned so a yfinance release can never silently change a gate.
# Bump ONLY deliberately, and re-run the equivalence test before committing the bump.
yfinance==0.2.65
pandas==2.2.3
numpy==2.1.3
requests==2.32.3
lxml==5.3.0
html5lib==1.1
beautifulsoup4==4.12.3
